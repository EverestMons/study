# study — Chunk Relationships Diagnostic
**Date:** 2026-03-22 | **Tier:** Small | **Execution:** Step 1 (DEV diagnostic)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/diagnostic-chunk-relationships-2026-03-22.md`. Execute Step 1, then stop.

---
---

## STEP 1 — DEV (Diagnostic)

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Skip glossary — this is a data model investigation task. **Task:** The CEO wants to understand whether chunks relate to each other, and whether the metadata captures those relationships. Investigate the full chunk data model and all inter-chunk connections. **Investigate:** (1) **Chunk table schema.** Read the chunks table definition in `src-tauri/migrations/001_v2_schema.sql`. List every column. Does any column reference another chunk (parent, sibling, next, previous)? Does `section_path` encode hierarchy (e.g., "5.1", "5.2" imply ordering within chapter 5)? Does `ordering` represent sequence within a material? (2) **Chunk-to-chunk relationships in the DB.** Search all migration files and `db.js` for any table or query that relates chunks to each other. Check: `chunk_fingerprints` (MinHash — does LSH create any similarity links between chunks?), `concept_links` / `facet_concept_links` (these link skills/facets, but do they indirectly link chunks?), any adjacency or sequence table. (3) **How chunks know their neighbors.** When the context builder loads chunks for a study session (`src/lib/study.js` — `loadFacetBasedContent`, `_keywordFallbackLoad`, or `buildFocusedContext`), does it load chunks in `ordering` sequence? Does it know chunk N's predecessor is chunk N-1? Can the AI tutor say "let's look at what comes before this section"? (4) **MinHash dedup — does it create relationships?** In `src/lib/minhash.js`, does the LSH comparison produce any stored similarity records between chunks? Or does it only produce a boolean "duplicate/not-duplicate" decision at extraction time with no persistent record? (5) **Section path hierarchy.** Do chunks from the same material share a structural hierarchy via `section_path`? E.g., chunk with path "5.1" and chunk with path "5.2" are siblings under chapter 5. Is this hierarchy used anywhere — context building, navigation, display? Or is `section_path` just metadata that sits unused? (6) **Cross-material chunk relationships.** Can chunks from different materials relate to each other? E.g., a textbook chunk and a lecture chunk covering the same topic. Is there any mechanism for this? Content hash dedup? Concept link transitivity (chunk A → skill → concept link → skill → chunk B)? (7) **What's missing.** Given the above findings, what chunk-to-chunk relationships COULD exist but DON'T? Consider: (a) sibling ordering (next/previous within a material), (b) hierarchical parent-child (chapter → section → subsection), (c) cross-material similarity (same topic in different materials), (d) prerequisite ordering (chunk A should be studied before chunk B), (e) supplementary relationships (chunk A is an example/exercise for chunk B's concept). **Report:** the complete chunk relationship model (what exists, what's implicit via section_path/ordering, what's accessible via skill/facet indirection, and what's genuinely missing). **Deposit:** `study/knowledge/research/chunk-relationships-diagnostic-2026-03-22.md`. After your output receipt, standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Commit with message: `"docs: chunk relationships diagnostic"`. **Final:** Move this diagnostic to Done: `mv study/knowledge/decisions/diagnostic-chunk-relationships-2026-03-22.md study/knowledge/decisions/Done/`. Plan complete — all steps executed.
