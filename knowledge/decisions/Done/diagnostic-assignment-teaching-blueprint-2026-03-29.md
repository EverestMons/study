# study — Assignment Teaching Flow Blueprint Prompt
**Date:** 2026-03-29 | **Type:** Diagnostic (SA blueprint request)

---

## STEP 1 — SA

---

> You are the Study Systems Analyst. Read your specialist file at `study/agents/STUDY_SYSTEMS_ANALYST.md` and the domain glossary at `study/knowledge/research/domain-glossary.md`. Read the diagnostic at `study/knowledge/research/assignment-teaching-flow-diagnostic-2026-03-29.md` for full current-state findings. **Blueprint two changes to the assignment mode teaching flow:**
>
> **Change 1 — Data-driven unlock gate.** Currently `parseQuestionUnlock()` in study.js (line ~1798) extracts `[UNLOCK_QUESTION]qN[/UNLOCK_QUESTION]` and StudyContext (line ~1372) honors it unconditionally. Add a code-level gate: before honoring the unlock, check the required skills for that question against current FSRS mastery. If ANY required skill's facets average below 60% retrievability, reject the unlock silently (do not set `unlocked: true`) and inject a system-level message into the conversation telling the AI: "Unlock rejected — skill [X] is at [Y]% mastery, below the 60% threshold. Continue teaching." The AI then continues teaching without the student seeing anything odd. **Blueprint must specify:** (a) where to read the required skills for a question (they're in `asgnWork.questions[n].requiredSkills`), (b) how to compute average facet retrievability per skill (which db.js methods to call — likely `FacetMastery.getBySkill()` or similar), (c) how to inject the rejection message back to the AI (likely append a system-turn to the messages array before the next API call), (d) what happens if the skill has no facets yet (no mastery data) — should default to locked or unlocked?
>
> **Change 2 — Answer submission assessment flow.** Currently when a question is unlocked, the student writes in a textarea and clicks "Mark done" which just sets `q.done = true`. Replace this with a two-phase flow: (a) Student writes answer and clicks "Submit for review" (replaces "Mark done"). (b) The AI receives the student's answer text as a user message like `[ANSWER_SUBMISSION q="qN"]student's answer text[/ANSWER_SUBMISSION]`. The system prompt instructs the AI to assess the answer against the question's required skills, emit `SKILL_UPDATE` ratings for relevant facets based on answer quality, and respond with either `[ANSWER_ACCEPTED]qN[/ANSWER_ACCEPTED]` (marks question done, moves to next) or teaching feedback explaining what's missing (student can revise and resubmit). (c) `parseAnswerResult()` in study.js extracts the accepted tag. StudyContext honors it by setting `q.done = true`. **Blueprint must specify:** (a) the new UI states in AssignmentPanel (writing → submitted → accepted, or writing → submitted → needs revision → writing), (b) the new parsing function, (c) the system prompt additions for answer assessment, (d) how the submitted answer text gets into the conversation messages.
>
> **Change 3 — Prompt hardening.** Strengthen the Answer Doctrine and Question Visibility Rules to be more resistant to the AI caving after wrong answers. Specifically: add an explicit instruction that after 2+ wrong student attempts, the AI should NOT reveal the answer but instead shrink the problem further or switch teaching angle. Add an instruction that the AI must NEVER state the direct answer to an assignment question — only teach the concepts that lead the student to discover it. Review the existing prompt text and propose exact additions/modifications. **Blueprint must provide the exact new prompt text, not just a description of what it should say.**
>
> **Constraints:** No schema changes — the unlock gate reads existing FSRS data, not new columns. `asgnWork` state changes are in-memory React state only. The 60% threshold is a constant that should be easy to adjust later (define it once, reference it). Default for skills with no facet data: locked (conservative — can't demonstrate mastery if there's no mastery data). **Deposit blueprint** to `knowledge/architecture/assignment-teaching-flow-blueprint-2026-03-29.md` using `with open("/Users/marklehn/Desktop/GitHub/study/knowledge/architecture/assignment-teaching-flow-blueprint-2026-03-29.md", "w") as f: f.write(content)`. Standard prompt feedback protocol → `knowledge/research/agent-prompt-feedback.md`.
