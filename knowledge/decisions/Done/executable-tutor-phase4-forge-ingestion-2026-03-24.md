# study + forge — Tutor Phase 4: Forge Ingestion of Tutor Session Data
**Date:** 2026-03-24 | **Tier:** Medium | **Execution:** Step 1 (SA) → Step 2 (STUDY DEV) → Step 3 (FORGE DEV) → Step 4 (QA)

## How to Run This Plan

Paste the following into Claude Code:

```
Read the plan at /Users/marklehn/Desktop/GitHub/study/knowledge/decisions/executable-tutor-phase4-forge-ingestion-2026-03-24.md. Execute Step 1. After completing Step 1, stop and wait for my confirmation before proceeding to Step 2.
```

---
---

## STEP 1 — STUDY SYSTEMS ANALYST (phase 4 blueprint)

---

> You are the Study Systems Analyst. Read specialist file at `study/agents/STUDY_SYSTEMS_ANALYST.md`. Read the roadmap at `study/knowledge/decisions/roadmap-tutor-facet-grounded-teaching-2026-03-24.md` (Phase 4 section). Read `study/knowledge/development/tutor-phase4-diagnostic-2026-03-24.md` for all findings — specifically: `fs:allow-write-file` is sandboxed to `$APPDATA/images/**` and `$APPDATA/tmp/**`; the session end convergence point is `saveSessionToJournal` in StudyContext.jsx where Phase 3's `updateChunkEffectiveness` was added; `generateSessionEntry()` returns a journal entry object with messageCount, topicsDiscussed, skillsUpdated, struggles, wins, masteryEvents, facetsAssessed. Blueprint three changes. **Change 1 — capabilities/default.json:** specify adding `$APPDATA/tutor-sessions/**` to both `fs:allow-write-file` and `fs:allow-mkdir` permission paths. This keeps the write sandboxed under APPDATA while enabling a new directory for tutor session summaries. **Change 2 — study.js `_updateTutorSessionSummary(sessionEntry, courseId, sessionId)`:** specify a new function that appends a session summary entry to a single accumulating markdown file at `$APPDATA/tutor-sessions/tutor-session-summary.md`. The function should: (a) use `BaseDirectory.AppData` from `@tauri-apps/plugin-fs` and lazy-import the plugin (same pattern as imageStore.js); (b) read the existing file if it exists (handle file-not-found gracefully — start fresh); (c) append one new H2 section for this session: `## Session {sessionId} — {date}` with sub-sections: Course ID, Facets Practiced (count from facetsAssessed), Skills Updated (from skillsUpdated array), Topics (from topicsDiscussed), Mastery Events (from masteryEvents if present), Struggles (from struggles if present). Each H2 section is one session — this is the Forge chunking boundary. (d) write the full updated content back to `$APPDATA/tutor-sessions/tutor-session-summary.md` using `writeTextFile` with `BaseDirectory.AppData`. (e) also call `mkdir` for `tutor-sessions/` with `recursive: true` before writing to ensure the directory exists. Specify where it is called: in `saveSessionToJournal` in StudyContext.jsx, after `updateChunkEffectiveness`, passing `entry` (the journal entry object), `active.id` (courseId), and `chatSessionId.current`. **Change 3 — Forge config.py and scanner.py:** specify adding `TUTOR_RESPONSE = "tutor_response"` to ChunkType enum. Add classification rule `("tutor-sessions/", ChunkType.TUTOR_RESPONSE)` before the generic `knowledge_deposit` fallback. Specify `_chunk_tutor_session(text)` in scanner.py — splits on H2 headers (`## Session`) same pattern as `_chunk_knowledge_deposit` but stamps `ChunkType.TUTOR_RESPONSE`. Specify the Forge scan path addition: the Forge scanner's `SCAN_PATHS` or equivalent config must include the `$APPDATA/tutor-sessions/` directory — specify the absolute path pattern using the macOS `$APPDATA` equivalent (`~/Library/Application Support/com.study.app/` or equivalent — SA should note this needs verification at runtime and suggest the DEV check the actual AppData path from the running app). Deposit: `study/knowledge/architecture/tutor-phase4-blueprint-2026-03-24.md`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 2 — STUDY DEVELOPER (tutor session summary writer)

---

> You are the Study Developer. Before starting, read `study/knowledge/architecture/tutor-phase4-blueprint-2026-03-24.md` and check Output Receipt status. If not Complete, stop and report. Read specialist file at `study/agents/STUDY_DEVELOPER.md`. Implement per blueprint exactly. Two changes: **(1) capabilities/default.json** — add `$APPDATA/tutor-sessions/**` to the path arrays for `fs:allow-write-file` and `fs:allow-mkdir`. Follow the exact JSON structure of the existing entries for `$APPDATA/images/**`. **(2) study.js `_updateTutorSessionSummary(sessionEntry, courseId, sessionId)`** — implement using lazy import of `@tauri-apps/plugin-fs` matching the exact pattern in `imageStore.js` (search for how imageStore.js imports writeFile and BaseDirectory). Use `mkdir` with `recursive: true` before every write. Read existing file content first with try/catch for file-not-found (return empty string on error). Append the new H2 section per blueprint spec. Write back with `writeTextFile`. Export the function. In `StudyContext.jsx`: import `_updateTutorSessionSummary`, add call in `saveSessionToJournal` after `updateChunkEffectiveness` — wrap in try/catch marked as non-critical (same pattern as the existing try/catch around `updateChunkEffectiveness`). Run `npx vite build --mode development` — confirm clean build. Also log the actual `$APPDATA` path at runtime by adding a one-time `console.log` in the function so the Forge DEV can verify the exact path during QA. Commit: `"feat: tutor phase 4 — session summary writer, $APPDATA/tutor-sessions/ capability, _updateTutorSessionSummary()"`. Deposit: `study/knowledge/development/tutor-phase4-dev-2026-03-24.md`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 3 — FORGE DEVELOPER (tutor_response chunk type + scan path)

---

> You are the Forge Developer. Before starting, read `study/knowledge/architecture/tutor-phase4-blueprint-2026-03-24.md` and `study/knowledge/development/tutor-phase4-dev-2026-03-24.md` — check both Output Receipt statuses. If either not Complete, stop and report. Read `forge/src/config.py` and `forge/src/scanner.py`. Three changes: **(1) config.py ChunkType enum** — add `TUTOR_RESPONSE = "tutor_response"` alongside existing types. **(2) config.py CLASSIFICATION_RULES** — add `("tutor-sessions/", ChunkType.TUTOR_RESPONSE)` before the final generic fallback rule. **(3) scanner.py** — add `_chunk_tutor_session(text)` that splits on `## Session` H2 headers (same pattern as `_chunk_knowledge_deposit` — split on `\n## `, stamp each chunk with `ChunkType.TUTOR_RESPONSE`). Add the dispatch case in `chunk_file()` for `ChunkType.TUTOR_RESPONSE`. For the scan path: add the macOS AppData path for the study app to Forge's scan config — the typical path is `~/Library/Application Support/com.tauri.study/tutor-sessions/` but the exact bundle identifier must be confirmed from `study/src-tauri/tauri.conf.json` (read the `identifier` field). Add this path to whatever list Forge uses to discover files outside the GitHub root (check `config.py` for `SCAN_ROOTS` or equivalent — if no such list exists, note it as a flag for CEO: Forge may need a scan root addition to pick up APPDATA files). Write Forge dev log using Python: `with open("/Users/marklehn/Desktop/GitHub/forge/knowledge/development/tutor-response-chunk-type-dev-2026-03-24.md", "w") as f: f.write(content)`. Run forge tests: `cd /Users/marklehn/Desktop/GitHub/forge && python -m pytest tests/ -q`. Commit: `"feat: forge — tutor_response chunk type, tutor-sessions/ classification rule, _chunk_tutor_session()"`. Standard prompt feedback protocol → `forge/knowledge/research/agent-prompt-feedback.md`.

---
---

## STEP 4 — STUDY SECURITY & TESTING ANALYST (phase 4 QA)

---

> You are the Study Security & Testing Analyst. Before starting, read `study/knowledge/development/tutor-phase4-dev-2026-03-24.md` and `forge/knowledge/development/tutor-response-chunk-type-dev-2026-03-24.md` — check both Output Receipt statuses. If either not Complete, stop and report. Read specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md`. Verify: (1) `capabilities/default.json` — confirm `$APPDATA/tutor-sessions/**` appears in both `fs:allow-write-file` and `fs:allow-mkdir` path arrays; (2) `_updateTutorSessionSummary()` exists in study.js — confirm it uses lazy plugin-fs import, `mkdir` with `recursive: true`, try/catch on read, and writes to `BaseDirectory.AppData`; (3) `StudyContext.jsx` `saveSessionToJournal` calls `_updateTutorSessionSummary` after `updateChunkEffectiveness` in a non-critical try/catch; (4) `forge/src/config.py` ChunkType enum contains `TUTOR_RESPONSE = "tutor_response"`; (5) `CLASSIFICATION_RULES` contains `("tutor-sessions/", ChunkType.TUTOR_RESPONSE)`; (6) `forge/src/scanner.py` contains `_chunk_tutor_session()` and dispatches it in `chunk_file()`; (7) build passes — `npx vite build --mode development`; (8) forge tests pass — `cd /Users/marklehn/Desktop/GitHub/forge && python -m pytest tests/ -q`. Flag if Step 3 could not confirm the Forge scan root addition — this determines whether Forge will actually pick up the APPDATA file or only classify it when manually pointed to it. Update `study/PROJECT_STATUS.md` — add milestone: "Tutor Phase 4 (2026-03-24): _updateTutorSessionSummary() writes to $APPDATA/tutor-sessions/tutor-session-summary.md at session end. Forge tutor_response chunk type added. Classification rule and chunker wired in Forge scanner. Full tutor feedback loop complete: facet assessment → session logging → chunk effectiveness → Forge ingestion." Move plan to Done: `mv study/knowledge/decisions/executable-tutor-phase4-forge-ingestion-2026-03-24.md study/knowledge/decisions/Done/`. Commit: `"chore: status update + move tutor-phase4 plan to Done"`. Deposit: `study/knowledge/qa/tutor-phase4-qa-2026-03-24.md`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`.

---
