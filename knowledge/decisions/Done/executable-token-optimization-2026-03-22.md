# study — Token Usage Optimization
**Date:** 2026-03-22 | **Tier:** Small | **Execution:** Step 1 (DEV) → Step 2 (QA)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/executable-token-optimization-2026-03-22.md`. Execute each step sequentially. After completing each step, stop and wait for user confirmation before proceeding. The plan has 2 steps.

---
---

## STEP 1 — DEV

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Read the diagnostic at `study/knowledge/research/token-usage-diagnostic-2026-03-22.md` for exact call sites and token estimates. **4 optimizations.** **Optimization 1 — Model downgrades (evaluation + flag only).** (a) In `src/lib/study.js`, find `evaluateAnswer` (line ~1898). It calls `callClaude` with the default model (Sonnet). Change to explicitly pass `model: "haiku"` (or however the app specifies model — check how `callClaude` accepts model selection, it may be via a parameter or by importing a different function. Check `src/lib/api.js` for the model parameter). (b) In `src/components/study/SkillsPanel.jsx`, find the flag re-examination call (line ~192). Same change — switch to Haiku. (c) Do NOT change `generateProblems` in study.js (~line 1843) — that stays on Sonnet for quality. **Optimization 2 — Facet concept link batching.** In `src/lib/conceptLinks.js`, find `generateFacetConceptLinks` (line ~205). Currently ALL new facets are sent in every batch against batched existing facets. Fix: batch new facets too. If there are 30 new facets and `FACET_BATCH_SIZE = 60`, batch new facets in groups of 30 (or use the same batch size). Each call compares one batch of new facets against one batch of existing facets. This reduces redundant new-facet tokens from N×(new facet list) to 1×(new facet list per batch). **Optimization 3 — Verification preview truncation.** In `src/lib/skills.js`, find `verifyDocument` (line ~254). The diagnostic says it sends 300-char previews of all chunks for multi-chunk materials. Reduce to 150-char previews — the model only checks readability, not deep content. Find the preview truncation point and halve it. **Optimization 4 — Assignment decomposition facet pre-filtering.** In `src/lib/skills.js`, find `decomposeAssignments` (line ~326). Currently sends the full facet list regardless of relevance. Add a pre-filter: extract topic keywords from the assignment text (split on whitespace, lowercase, remove stopwords), then filter the facet list to only facets whose `name` or `category` contains at least one keyword. Include a fallback — if the filtered list has fewer than 10 facets, send the full list (the filtering was too aggressive). **Constraints:** Do not change tutoring calls (boot session, send message). Do not change extraction pipeline calls (extractChapter, enrichFromMaterial) — those are already on Haiku and optimized. Do not change problem generation model — stays Sonnet. Build verify: `cd study && npx vite build --mode development`. Commit with message: `"feat: token optimization — model downgrades + batching + truncation + pre-filter"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 2 (QA). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 2 — QA

---

> You are the Study Security & Testing Analyst. Read your specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md` first. Read the diagnostic at `study/knowledge/research/token-usage-diagnostic-2026-03-22.md` for the original call site inventory. **Verification areas:** (1) **Model downgrades** — read `src/lib/study.js` `evaluateAnswer` and `src/components/study/SkillsPanel.jsx` flag handler. Verify both now use Haiku (not Sonnet). Verify `generateProblems` in study.js STILL uses Sonnet. Expected: evaluation + flag on Haiku, problems on Sonnet. (2) **Facet concept link batching** — read `src/lib/conceptLinks.js` `generateFacetConceptLinks`. Verify new facets are batched (not sent in full on every call). Count: if 30 new + 200 existing, how many calls? Should be < old count. Expected: new facets batched, fewer total calls. (3) **Verification preview truncation** — read `src/lib/skills.js` `verifyDocument`. Verify chunk previews are truncated to ~150 chars (not 300). Expected: shorter previews. (4) **Assignment facet pre-filtering** — read `src/lib/skills.js` `decomposeAssignments`. Verify facet list is keyword-filtered before inclusion in prompt. Verify fallback: if filtered list < 10, full list is sent. Expected: pre-filter present with fallback. (5) **Tutoring calls unchanged** — verify `callClaudeStream` in StudyContext (boot + send) still uses Sonnet/default. Expected: no model changes. (6) **Extraction calls unchanged** — verify `callClaude` in extraction.js still uses Haiku for extractChapter and enrichFromMaterial. Expected: no changes. (7) **Build verification.** **Deposit:** `study/knowledge/qa/token-optimization-qa-2026-03-22.md` with per-area PASS/FAIL. **Final:** Move this plan to Done: `mv study/knowledge/decisions/executable-token-optimization-2026-03-22.md study/knowledge/decisions/Done/`. Commit: `"chore: token optimization complete"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Plan complete — all steps executed.
