# study — Materials UX Fixes (Reclassify + Progress + Duplicate Modal)
**Date:** 2026-03-22 | **Tier:** Small | **Execution:** Step 1 (DEV) → Step 2 (QA)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/executable-materials-ux-fixes-2026-03-22.md`. Execute each step sequentially. After completing each step, stop and wait for user confirmation before proceeding. The plan has 2 steps.

---
---

## STEP 1 — DEV

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Read the diagnostic at `study/knowledge/research/materials-ux-fixes-diagnostic-2026-03-22.md` for exact file locations and existing methods. **3 fixes in MaterialsScreen.jsx + StudyContext.jsx.** **Fix 1 — Reclassify uploaded materials.** In `MaterialsScreen.jsx` `renderExpandedDetail()`, near the existing classification badge (line ~174), add a reclassify control. Use small pill buttons showing all 7 classification types (`CLS` array), styled like the staging area classify buttons (from the staging redesign). The current classification is highlighted. Clicking a different type calls `Materials.update(mat.id, { classification: newType })`, then refreshes the course data (`loadCoursesNested` → `setCourses` → `setActive`). The material moves to its new group in the grid. **Fix 2 — Extraction progress on MaterialsScreen.** The `ExtractionProgress` component is hidden on MaterialsScreen via `screen !== "materials"` guard in `ScreenRouter.jsx:103`. Instead of removing that guard (which would create redundancy with per-card progress), add an inline progress summary at the top of MaterialsScreen's materials section (above the tab filters). Only visible when `bgExtraction` is active. Show: "Extracting: [current material name] (N of M)" with a compact progress bar and a cancel button. Use the same `bgExtraction` state from `useStudy()`. Position it between the staging container and the "Course Materials" header. Style: `T.sf` background, `T.bd` border, 12px rounded, compact (single line with padding 8px 16px). **Fix 3 — Duplicate material modal.** In `StudyContext.jsx`, find `filterDuplicates()` (line ~408). Currently it calls `addNotif("warn", ...)` for duplicates. Replace with: set a new state `duplicateAlert` (new `useState(null)`) with the duplicate filenames. In `MaterialsScreen.jsx`, render a modal when `duplicateAlert` is set — centered overlay (same pattern as `errorLogModal`), showing "Material already uploaded" as header, list of duplicate filenames, an "OK" button that clears `duplicateAlert`. Remove the `addNotif("warn", "Skipped duplicate...")` call — the modal replaces it. Also update the DB-level duplicate path in StudyContext (`addMats` flow, line ~1406) to use the same `duplicateAlert` state instead of `addNotif`. **Constraints:** Do not change the DB schema. Do not change chunking or extraction logic. `Materials.update()` already exists and accepts `classification` — no new DB methods needed. The `duplicateAlert` state should be exposed via `useStudy()` so MaterialsScreen can read it. Build verify: `cd study && npx vite build --mode development`. Commit with message: `"feat: material reclassify + extraction progress inline + duplicate modal"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 2 (QA). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 2 — QA

---

> You are the Study Security & Testing Analyst. Read your specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md` first. Read the diagnostic at `study/knowledge/research/materials-ux-fixes-diagnostic-2026-03-22.md` for context. **Verification areas:** (1) **Reclassify control** — read `MaterialsScreen.jsx` `renderExpandedDetail`. Verify: 7 classification pill buttons present, current classification highlighted, clicking calls `Materials.update()` with correct args, course data refreshes after update, material moves to new group. Expected: PASS. (2) **Reclassify downstream** — verify that after reclassification, the material's group header in the grid updates, the classification badge updates, and the trust label updates. The diagnostic confirms binding quality scores update automatically via runtime JOIN. Expected: display updates reactively. (3) **Extraction progress inline** — verify MaterialsScreen renders a compact progress bar when `bgExtraction` is active. Verify it shows current material name and N/M progress. Verify cancel button works. Verify it's hidden when no extraction is running. Expected: PASS. (4) **ExtractionProgress guard unchanged** — verify `ScreenRouter.jsx` still has `screen !== "materials"` guard on the floating ExtractionProgress component. The inline progress on MaterialsScreen is separate. Expected: guard still present, no redundancy. (5) **Duplicate modal — client-side path** — verify `filterDuplicates` in StudyContext sets `duplicateAlert` state instead of calling `addNotif`. Verify MaterialsScreen renders the modal with "Material already uploaded" header, filename list, OK button. Expected: modal appears, no notification toast. (6) **Duplicate modal — DB-level path** — verify the `addMats` flow (~line 1406) also uses `duplicateAlert` instead of `addNotif` for deduplicated materials. Expected: both paths use modal, not toast. (7) **Build verification.** **Deposit:** `study/knowledge/qa/materials-ux-fixes-qa-2026-03-22.md` with per-area PASS/FAIL. **Final:** Move this plan to Done: `mv study/knowledge/decisions/executable-materials-ux-fixes-2026-03-22.md study/knowledge/decisions/Done/`. Commit: `"chore: materials UX fixes complete"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Plan complete — all steps executed.
