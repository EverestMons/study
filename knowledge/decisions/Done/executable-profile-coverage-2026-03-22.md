# study — Profile Coverage Indicator
**Date:** 2026-03-22 | **Tier:** Small | **Execution:** Step 1 (DEV) → Step 2 (QA)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/executable-profile-coverage-2026-03-22.md`. Execute each step sequentially. After completing each step, stop and wait for user confirmation before proceeding. The plan has 2 steps.

---
---

## STEP 1 — DEV

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Read the diagnostic at `study/knowledge/research/profile-skill-saving-diagnostic-2026-03-22.md` — it maps the full data chain from FSRS update to profile display. **Task:** Add a coverage indicator alongside the existing readiness display on the profile. The CEO wants both metrics visible: readiness (how well you remember what you've studied) AND coverage (how much of the skill you've studied). **Change 1 — Compute coverage per sub-skill in `StudyContext.jsx` `loadProfile()`.** The data is already loaded: `facetsBySkill[sub.id]` gives all facets, `facetMasteryById[f.id]` gives which have mastery. Compute: `testedFacetCount = facetsBySkill[sub.id].filter(f => facetMasteryById[f.id]).length`, `totalFacetCount = facetsBySkill[sub.id].length`, `coverage = totalFacetCount > 0 ? testedFacetCount / totalFacetCount : 0`. Attach to the enriched sub-skill object: `coverage`, `testedFacetCount`, `totalFacetCount`. **Change 2 — Compute coverage per parent skill.** In the parent enrichment (line ~865), aggregate: `parentCoverage = sum of sub-skill coverages / acquiredSubs.length` (or average of individual coverages). Also compute: `parentTestedFacets = sum of testedFacetCount`, `parentTotalFacets = sum of totalFacetCount`. Attach to the parent enrichment object. **Change 3 — Display coverage on ProfileScreen.** Two locations: (a) **Parent skill card** (collapsed view, line ~188): below the existing readiness bar, add a second thinner bar showing coverage. Label: "N/M facets" in muted text to the right. Coverage bar color: `T.ac` (accent blue) at reduced opacity to differentiate from the readiness bar (which uses green/amber/red). (b) **Sub-skill expanded detail** (line ~390): add "Coverage: N/M facets (X%)" line near the existing mastery stats. Use a compact inline bar or just text. **Change 4 — Domain hero view.** The parent's readiness bar on the hero domain grouping already shows average readiness. Add a small "N/M facets" label next to the readiness percentage, using the parent's aggregated facet counts. Keep it compact — this is secondary information. **Constraints:** Do not change FSRS logic. Do not change the readiness computation or display — coverage is additive, not a replacement. Do not change how `applySkillUpdates` computes aggregates. The coverage data comes from the existing `facetsBySkill` and `facetMasteryById` maps already loaded in `loadProfile()` — no new DB queries needed. Build verify: `cd study && npx vite build --mode development`. Commit with message: `"feat: profile coverage indicator — facet coverage alongside readiness"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 2 (QA). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 2 — QA

---

> You are the Study Security & Testing Analyst. Read your specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md` first. Read the diagnostic at `study/knowledge/research/profile-skill-saving-diagnostic-2026-03-22.md` for the data chain. **Verification areas:** (1) **Coverage computation — sub-skill level.** Read `StudyContext.jsx` `loadProfile()`. Verify `testedFacetCount`, `totalFacetCount`, and `coverage` are computed per sub-skill from the existing `facetsBySkill` and `facetMasteryById` maps. Verify: a skill with 5 facets and 1 tested → coverage = 0.2. A skill with 0 facets → coverage = 0. Expected: correct computation with edge cases handled. (2) **Coverage computation — parent level.** Verify parent-level facet aggregation. A parent with 2 sub-skills (sub A: 3/5 tested, sub B: 1/3 tested) → parentTestedFacets = 4, parentTotalFacets = 8, parentCoverage = 0.5. Expected: sums correct. (3) **ProfileScreen display — parent card.** Read `ProfileScreen.jsx`. Verify a second bar (coverage) appears below the readiness bar on parent cards. Verify "N/M facets" label is present. Verify coverage bar uses a distinct color from readiness bar. Expected: both bars visible, visually distinct. (4) **ProfileScreen display — sub-skill detail.** Verify coverage information appears in expanded sub-skill view. Expected: "Coverage: N/M facets (X%)" or equivalent present. (5) **Edge case: skill with 0 facets.** Verify no division by zero, coverage shows 0 or is hidden gracefully. Expected: no crash, reasonable display. (6) **Edge case: skill with all facets tested.** Coverage = 100%. Verify both readiness and coverage bars render correctly at 100%. Expected: no overflow or layout issues. (7) **Readiness unchanged.** Verify the existing readiness bar computation and display are completely untouched — coverage is additive. Expected: zero changes to readiness logic. (8) **Build verification.** **Deposit:** `study/knowledge/qa/profile-coverage-qa-2026-03-22.md` with per-area PASS/FAIL. **Final:** Move this plan to Done: `mv study/knowledge/decisions/executable-profile-coverage-2026-03-22.md study/knowledge/decisions/Done/`. Commit: `"chore: profile coverage indicator complete"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Plan complete — all steps executed.
