# study — Token Usage Diagnostic (Extraction + Chunking)
**Date:** 2026-03-22 | **Tier:** Small | **Execution:** Step 1 (DEV diagnostic)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/diagnostic-token-usage-2026-03-22.md`. Execute Step 1, then stop.

---
---

## STEP 1 — DEV (Diagnostic)

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Skip glossary — this is a performance/cost investigation task. **Task:** The CEO wants to reduce token usage during material extraction and chunk creation. Investigate every point where Claude API tokens are consumed. **Investigate:** (1) **All `callClaude` and `callClaudeStream` call sites.** Search entire `src/` for every `callClaude(` and `callClaudeStream(` invocation. For each, report: file, line, function name, what it's doing (extraction, teaching, concept links, etc.), which model it uses (Sonnet, Haiku, etc.), and estimate the prompt size (system prompt tokens + context tokens + user message tokens). Categorize each as: extraction pipeline, teaching/tutoring, or utility. (2) **Extraction pipeline token flow.** Trace the full extraction flow in `src/lib/extraction.js` and `src/lib/skills.js`. For each API call: what goes into the prompt (system instructions, chunk content, structural metadata, CIP taxonomy list, existing skills for dedup), how many chunks are processed per call, and whether any of this content could be reduced without losing extraction quality. The CIP taxonomy list is reportedly ~4,300 tokens on first-chapter extraction — is it sent on every call or only the first? (3) **Concept link generation.** In `src/lib/conceptLinks.js`, how many API calls are made per extraction? Is it one call per skill pair, one call per parent group, or batched? What's the prompt size per call? What model is used? (4) **Chunking pipeline.** Does chunking itself use any API tokens? Or is it purely deterministic (parser + heading detection + size splitting)? Confirm there are no LLM calls in `chunker.js`, `epubParser.js`, `docxParser.js`, `pdfParser.js`. (5) **Syllabus parsing.** `syllabusParser.js` uses Haiku for extraction. What's the prompt size? How many calls per syllabus? (6) **Assignment decomposition.** Does `decomposeAssignments` in `skills.js` make API calls? What's the prompt size? (7) **Token waste opportunities.** For each API call identified, assess: (a) Is the system prompt larger than necessary? Are there instructions being sent that don't apply to this specific call? (b) Is context being sent that the model doesn't need for this task? (c) Could multiple small calls be batched into one? (d) Could a cheaper model (Haiku instead of Sonnet) handle this task? (e) Is the CIP taxonomy list (~4,300 tokens) being sent when it shouldn't be? (f) Are chunk contents being sent in full when a summary would suffice? (g) Is the structural metadata being sent when the model doesn't use it? **Report:** a table of every API call with model, estimated input tokens, estimated output tokens, frequency (per material, per chunk, per skill, per session), and waste assessment. Rank opportunities by estimated token savings. **Deposit:** `study/knowledge/research/token-usage-diagnostic-2026-03-22.md`. After your output receipt, standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Commit with message: `"docs: token usage diagnostic for extraction pipeline"`. **Final:** Move this diagnostic to Done: `mv study/knowledge/decisions/diagnostic-token-usage-2026-03-22.md study/knowledge/decisions/Done/`. Plan complete — all steps executed.
