# study — Dynamic Input (Math Mode + AI-Driven Mode Switching)
**Date:** 2026-03-22 | **Tier:** Medium | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (DEV) → Step 4 (QA) → Step 5 (UXV)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/executable-dynamic-input-2026-03-22.md`. Execute each step sequentially. After completing each step, stop and wait for user confirmation before proceeding. The plan has 5 steps.

---
---

## STEP 1 — SA (Blueprint)

---

> You are the Study Systems Analyst. Read your specialist file at `study/agents/STUDY_SYSTEMS_ANALYST.md` first. Then read the diagnostic at `study/knowledge/research/dynamic-input-diagnostic-2026-03-22.md` — it maps every component, state variable, and integration point. Then read the full spec at `study/docs/planning/dynamic-input-spec.md`. Then read `src/components/study/InputBar.jsx` (147 lines) and `src/components/study/CodeEditor.jsx` (187 lines). **Task:** Blueprint the dynamic input feature across 4 changes. CodeEditor is already complete — no changes needed there. **Change 1 — `inputMode` enum state.** Replace `codeMode` boolean in StudyContext with `inputMode` enum (`"text"` | `"code"` | `"math"`). Specify: every location where `codeMode` is read or set (the diagnostic lists them), the migration from `setCodeMode(true)` → `setInputMode("code")`, how `detectedLanguage` state works alongside the new enum, and what the default is ("text"). The `useMemo` dependency array on the context value needs updating. **Change 2 — Math toolbar component.** New `MathToolbar.jsx` component rendered in InputBar when `inputMode === "math"`. Specify: the symbol groups (Greek, Operators, Calculus, Superscript/subscript, Arrows, Misc — per spec), the insertion mechanism (ref to textarea + `selectionStart` manipulation), the collapsible toggle behavior, the visual styling (should match theme tokens — small pills similar to classify buttons on MaterialsScreen). The toolbar sits above the textarea, not as a floating popover. Specify the full symbol list per group. **Change 3 — `[INPUT_MODE]` tag parsing + AI-driven switching.** New parser function in `study.js` following the `parseSkillUpdates`/`parseImageTags` pattern. Specify: the regex, the call site in StudyContext where AI responses are processed (find where `parseSkillUpdates` is called — the `[INPUT_MODE]` parser goes next to it), how the parsed mode flows to `setInputMode`. Also specify the system prompt addition — the exact text block to add to `buildSystemPrompt()` telling the AI when and how to emit `[INPUT_MODE]` tags. The tag should be stripped from displayed text (same as SKILL_UPDATE tags). **Change 4 — Mode persistence + manual override.** Currently code mode resets on submit (`StudyContext.jsx:1211`). Change: mode persists until the AI or user explicitly changes it. The `</>` toggle button in InputBar becomes a 3-way selector. Specify: the UI for 3-way selection (3 small icon buttons? a segmented control? a cycle button that rotates through modes?), keyboard shortcuts (Ctrl+Shift+C for code exists — add Ctrl+Shift+M for math?), and how manual override interacts with AI-driven switching (manual always wins, AI suggestion is applied only if user hasn't manually overridden in this session). **Blueprint must include** the full symbol list, the system prompt text, the `inputMode` state migration checklist, and a "How to verify" section. **Deposit:** `study/knowledge/architecture/dynamic-input-blueprint-2026-03-22.md`. Next is Step 2 (DEV). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 2 — DEV (State refactor + math toolbar)

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Read the SA blueprint at `study/knowledge/architecture/dynamic-input-blueprint-2026-03-22.md` — implement Changes 1 and 2 as a checklist. **Change 1 — `inputMode` enum:** Replace `codeMode` boolean with `inputMode` state (`"text"` default) in StudyContext. Migrate every `codeMode` read/set site listed in the blueprint. Update the `useMemo` context value. Update `InputBar.jsx` conditional from `codeMode ? <CodeEditor> : <textarea>` to a 3-way switch on `inputMode`. **Change 2 — Math toolbar:** Create `src/components/study/MathToolbar.jsx` per blueprint specs. Wire into InputBar — render above the textarea when `inputMode === "math"`. Implement Unicode insertion at cursor position using the textarea ref. Implement collapsible toggle. Style with theme tokens. **Also:** Update the `</>` toggle button to a 3-way mode selector per the blueprint's UI spec. Add Ctrl+Shift+M keyboard shortcut for math mode. **Constraints:** Do not modify CodeEditor.jsx — it's already complete. Do not change FSRS logic. Do not change the system prompt yet (that's Step 3). The mode selector must work for manual switching even before AI-driven switching is wired. Build verify: `cd study && npx vite build --mode development`. Commit with message: `"feat: inputMode enum + math toolbar + 3-way mode selector"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 3 (DEV). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 3 — DEV (AI-driven switching + mode persistence)

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Read the SA blueprint at `study/knowledge/architecture/dynamic-input-blueprint-2026-03-22.md` — implement Changes 3 and 4. **Change 3 — `[INPUT_MODE]` parsing:** Add `parseInputMode(text)` function in `src/lib/study.js` following the `parseSkillUpdates` regex pattern. Find where `parseSkillUpdates` is called in StudyContext (in the message processing flow after AI response arrives). Add `parseInputMode` call at the same location. When a mode is detected, call `setInputMode(parsedMode)`. Strip the `[INPUT_MODE: ...]` tag from the displayed message text (same stripping approach as SKILL_UPDATE). **System prompt addition:** Add the INPUT_MODE instruction block to `buildSystemPrompt()` per the blueprint's exact text. Place it near the SKILL_UPDATE instructions since they follow the same tag pattern. **Change 4 — Mode persistence:** Remove the `setCodeMode(false)` (now `setInputMode("text")`) call on message submit in StudyContext. Mode stays until AI or user changes it. Add auto-detection for math mode at session start in `bootWithFocus` — if course/skill name contains math-related keywords (Calculus, Algebra, Statistics, Linear Algebra, Differential Equations, etc.), set `inputMode` to `"math"`. The existing `detectLanguage` → code mode logic stays, math detection is a parallel check. **Constraints:** Do not change CodeEditor.jsx. Do not change FSRS logic. The `[INPUT_MODE]` tag must be invisible to the user (stripped from displayed text). Build verify. Commit with message: `"feat: AI-driven input mode switching + mode persistence + math auto-detect"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 4 (QA). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 4 — QA

---

> You are the Study Security & Testing Analyst. Read your specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md` first. Read the SA blueprint at `study/knowledge/architecture/dynamic-input-blueprint-2026-03-22.md` for acceptance criteria. **Verification areas:** (1) **`inputMode` state** — verify `codeMode` boolean is fully replaced. Search entire `src/` for any remaining `codeMode` references. Verify `inputMode` is in the context `useMemo` dependency array. Expected: zero `codeMode` references remain. (2) **Math toolbar** — read `src/components/study/MathToolbar.jsx`. Verify all symbol groups from the blueprint are present. Verify insertion mechanism uses textarea ref + selectionStart. Verify collapsible toggle works. Expected: all groups present, insertion functional. (3) **3-way mode selector** — verify InputBar renders the correct input component for each mode: text→textarea, code→CodeEditor, math→textarea+MathToolbar. Verify manual switching works (click + keyboard shortcuts). Expected: all 3 modes render correctly. (4) **`[INPUT_MODE]` parsing** — read `src/lib/study.js`, find `parseInputMode`. Verify regex matches `[INPUT_MODE: code:python]`, `[INPUT_MODE: math]`, `[INPUT_MODE: text]`. Verify the tag is stripped from displayed text. Verify the parser is called in the message processing flow. Expected: parser present, stripping works. (5) **System prompt** — verify INPUT_MODE instruction block exists in `buildSystemPrompt()`. Verify it tells the AI when to emit each mode tag. Expected: prompt block present. (6) **Mode persistence** — verify mode does NOT reset on message submit. Verify mode persists across messages until explicitly changed. Expected: no `setInputMode("text")` in submit handler. (7) **Math auto-detection** — verify `bootWithFocus` detects math-related course/skill names and sets `inputMode` to `"math"`. Expected: math keywords trigger math mode. (8) **Code mode regression** — verify CodeEditor still works: syntax highlighting, line numbers, language badge, submit via Cmd+Enter, Escape to exit. Expected: zero regressions. (9) **Build verification.** **Deposit:** `study/knowledge/qa/dynamic-input-qa-2026-03-22.md` with per-area PASS/FAIL. Commit with message: `"docs: dynamic input QA"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 5 (UXV). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 5 — UXV

---

> You are the Study UX Validator. Read your specialist file at `study/agents/STUDY_UX_VALIDATOR.md` first. Read the QA report at `study/knowledge/qa/dynamic-input-qa-2026-03-22.md`. Read `src/components/study/InputBar.jsx` and `src/components/study/MathToolbar.jsx` post-implementation. **Validation areas:** (1) **Mode selector discoverability** — is the 3-way selector intuitive? Can a user figure out how to switch between text/code/math without instructions? Are the icons/labels clear? (2) **Math toolbar usability** — are the symbol groups logically organized? Can a student find the symbol they need quickly? Is the toolbar compact enough to not dominate the input area? Is the collapsible toggle discoverable? (3) **AI-driven switching feel** — when the AI emits `[INPUT_MODE: math]` and the input switches, does it feel helpful or jarring? Is the transition smooth? Does the student understand why the input changed? (4) **Mode persistence** — does it feel right that the mode stays after sending a message? Or does it create confusion when the student switches topics mid-session? (5) **Math input ergonomics** — can a student type a calculus expression (e.g., "∫₀¹ x² dx = 1/3") using the toolbar efficiently? Is the click-to-insert flow fast enough, or does it feel tedious compared to just typing? (6) **Learning science risk** — LOW (input modality, not learning interaction). Note: could math mode discourage students from writing verbal explanations alongside equations? The AI should still expect verbal reasoning, not just symbols. **Deposit:** `study/knowledge/design/validation/dynamic-input-uxv-2026-03-22.md`. **Final:** Move this plan to Done: `mv study/knowledge/decisions/executable-dynamic-input-2026-03-22.md study/knowledge/decisions/Done/`. Commit: `"chore: dynamic input complete"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Plan complete — all steps executed.
