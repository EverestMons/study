# study — Assignment Mode FSRS Update Flow Diagnostic
**Date:** 2026-03-29 | **Type:** Diagnostic

---

## STEP 1 — DEV

---

> You are the Study Developer. Skip specialist file and glossary reads — this is a code-tracing task. **Investigate whether FSRS mastery is actively updated during assignment mode tutoring, and trace the unlock attempt flow.** (1) In StudyContext.jsx, find where AI responses are processed after each message exchange. Trace whether `parseSkillUpdates()` is called on AI responses during assignment mode — or is it only called in other modes? Show the exact code path from receiving an AI response to calling `applySkillUpdates()`. (2) Check `applySkillUpdates()` in StudyContext.jsx — does it actually write FSRS mastery updates to the DB during assignment mode? Or is there a mode guard that skips it? (3) Trace the unlock attempt: when the AI emits `[UNLOCK_QUESTION]qN[/UNLOCK_QUESTION]`, find the code that handles it. Does it call `computeFacetReadiness()`? If yes, trace what `computeFacetReadiness()` reads — does it read the LATEST FSRS data that was just written by `applySkillUpdates()`, or could there be a stale cache? Specifically: if `applySkillUpdates()` writes new mastery data in the same response processing cycle, does `computeFacetReadiness()` see it? (4) Check if there's a timing issue: are `parseSkillUpdates` + `applySkillUpdates` called BEFORE or AFTER `parseQuestionUnlock` + the unlock gate? If the unlock check happens before mastery is written, the gate will always see stale data. (5) Check `computeFacetReadiness()` in study.js — does it query the DB directly each time, or does it use cached skill data from `cachedSessionCtx.current.skills`? If it uses cached data, when does that cache get refreshed? (6) Check `unlockRejectionRef` — is the rejection message actually being injected into the next API call? Trace from `unlockRejectionRef.current` being set to where it's read in `sendMessage()`. **Deposit findings** to `knowledge/research/assignment-fsrs-update-flow-diagnostic-2026-03-29.md` using `with open("/Users/marklehn/Desktop/GitHub/study/knowledge/research/assignment-fsrs-update-flow-diagnostic-2026-03-29.md", "w") as f: f.write(content)`. Standard prompt feedback protocol → `knowledge/research/agent-prompt-feedback.md`.
