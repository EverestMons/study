# study — Chunk Metadata Enrichment
**Date:** 2026-03-22 | **Tier:** Medium | **Execution:** Step 1 (SA) → Step 2 (DEV) → Step 3 (QA)

## How to Run This Plan

Paste this into Claude Code:

Read the orchestration plan at `study/knowledge/decisions/executable-chunk-metadata-enrichment-2026-03-22.md`. Execute each step sequentially. After completing each step, stop and wait for user confirmation before proceeding. The plan has 3 steps.

---
---

## STEP 1 — SA (Blueprint)

---

> You are the Study Systems Analyst. Read your specialist file at `study/agents/STUDY_SYSTEMS_ANALYST.md` first. Then read the diagnostic at `study/knowledge/research/chunk-metadata-diagnostic-2026-03-22.md` — this contains the current state of all metadata fields, which parsers produce them, and the 8 identified gaps. Then read `src/lib/htmlToMarkdown.js` (focus on `computeSectionMetadata` at lines 434-469 and the DOM walk metadata tracking), and `src/lib/chunker.js` (focus on `splitLargeSections` and `mergeSmallSections`). **Task:** Produce a blueprint for 4 metadata enrichment changes. **Change 1 — Add `blockquote_count`.** Add to both the DOM walk path in `htmlToMarkdown.js` (increment on `<blockquote>` during HTML-to-markdown conversion) and the `computeSectionMetadata` regex path (detect markdown blockquote lines starting with `> `). Specify the exact regex pattern and where in the DOM walk to increment. **Change 2 — Add `subsection_count` + `subsections[]`.** These track sub-headings within a chunk. In the regex path: scan for markdown headings (`## `, `### `, etc.) at one level deeper than the chunk's own heading_level. In the DOM walk: track during section building. Specify: what heading levels count as "subsections" relative to the chunk's split level, and what the `subsections` array contains (heading text strings). **Change 3 — Fix `list_count` to actual count + split `ol`/`ul`.** Replace the binary `list_count: 0|1` with `ordered_list_count` and `unordered_list_count` as actual counts. In the DOM walk: increment on `<ol>` and `<ul>` tags separately. In the regex path: count sequences starting with `N. ` (ordered) vs `- ` (unordered) — a new sequence starts after a non-list line. Specify the exact regex or detection logic. **Change 4 — Fix metadata inflation on oversized section splits.** In `chunker.js`, `splitLargeSections` currently spreads the parent metadata to each sub-section. Instead, after splitting, recompute metadata for each sub-section by calling `computeSectionMetadata(subSection.content)`. This requires importing `computeSectionMetadata` into chunker.js (it's currently only used in the parser files). Specify the exact import and the location in the split logic. **Also specify:** the updated metadata schema (full field list with types), how `extraction.js` `aggregateMetadata()` should handle the new fields (it needs to sum the new counts and collect subsection arrays), and the downstream LLM prompt changes in `extraction.js` to inject blockquote and subsection data. **Blueprint must include** a "How to verify" section with acceptance criteria per change. **Deposit:** `study/knowledge/architecture/chunk-metadata-enrichment-blueprint-2026-03-22.md`. Next is Step 2 (DEV). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 2 — DEV (Implementation)

---

> You are the Study Developer. Read your specialist file at `study/agents/STUDY_DEVELOPER.md` first. Then read the SA blueprint at `study/knowledge/architecture/chunk-metadata-enrichment-blueprint-2026-03-22.md` — implement as a checklist against blueprint sections. **Files to modify:** (1) `src/lib/htmlToMarkdown.js` — add `blockquote_count` to DOM walk metadata tracking + update `computeSectionMetadata()` with blockquote regex, subsection detection, and fixed list counting (ordered/unordered separate, actual count not binary). (2) `src/lib/chunker.js` — import `computeSectionMetadata` from `htmlToMarkdown.js`, recompute metadata for sub-sections in `splitLargeSections()` instead of inheriting parent metadata. (3) `src/lib/extraction.js` — update `aggregateMetadata()` to handle new fields (`blockquote_count`, `subsection_count`, `subsections`, `ordered_list_count`, `unordered_list_count`), update LLM prompt structural analysis section to inject blockquote and subsection data. **Constraints:** Do not modify any parser files (`epubParser.js`, `docxParser.js`, `pdfParser.js`) — they already call `computeSectionMetadata()` and will pick up the changes automatically. Do not modify the DB schema — `structural_metadata` is a JSON TEXT column, new fields are backward compatible. Do not break existing metadata fields — all current fields remain, new fields are additive. Existing chunks in the DB with old-format metadata will still work (consumers must handle missing fields gracefully). Build verify: `cd study && npx vite build --mode development`. Commit with message: `"feat: chunk metadata enrichment — blockquote, subsection, list count, split fix"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Next is Step 3 (QA). Stop here and wait for user confirmation before proceeding.

---
---

## STEP 3 — QA

---

> You are the Study Security & Testing Analyst. Read your specialist file at `study/agents/STUDY_SECURITY_TESTING_ANALYST.md` first. Read the SA blueprint at `study/knowledge/architecture/chunk-metadata-enrichment-blueprint-2026-03-22.md` for acceptance criteria. **Verification areas:** (1) **`computeSectionMetadata` output** — create a test markdown string with 2 blockquotes, 3 ordered list items, 2 unordered list items, 1 subsection heading, and verify the function returns correct counts for all fields including new ones (`blockquote_count: 2`, `ordered_list_count: 1`, `unordered_list_count: 1`, `subsection_count: 1`). Expected: all new fields present with correct counts. (2) **Backward compatibility** — verify that metadata objects missing new fields (simulating old DB data) don't crash `aggregateMetadata()` in extraction.js. Missing fields should default to 0/empty. Expected: no errors with old-format metadata. (3) **Split inflation fix** — verify that `splitLargeSections` in chunker.js produces sub-sections with recomputed metadata, not inherited parent metadata. Create a test case: one section with 10 bold terms split into 2 parts, each part should have its own bold_term_count (not 10 for both). Expected: sub-section metadata reflects actual sub-section content. (4) **LLM prompt injection** — verify that `extraction.js` prompt includes blockquote and subsection data when present, and omits them gracefully when metadata is null or missing those fields. Expected: prompt renders correctly with and without new fields. (5) **Build verification** — `npx vite build --mode development` passes. **Deposit:** `study/knowledge/qa/chunk-metadata-enrichment-qa-2026-03-22.md` with per-area PASS/FAIL. **Final:** Move this plan to Done: `mv study/knowledge/decisions/executable-chunk-metadata-enrichment-2026-03-22.md study/knowledge/decisions/Done/`. Commit: `"chore: chunk metadata enrichment complete"`. Standard prompt feedback protocol → `study/knowledge/research/agent-prompt-feedback.md`. Plan complete — all steps executed.
