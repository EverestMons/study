# Walk register — `claude-cli-backend-2026-09-03` (study)

**schema_version:** `0.3`

**Plan:** `scratchpad/draft-diagnostic-claude-cli-backend-2026-09-03.md` (stable slug; deposits to `study/knowledge/decisions/` as `ready-diagnostic-draft-<HHMMSS>.md`)
**Tier:** T1 — firing triggers T-3 (new execution environment: a spawned local CLI process inside a packaged .app), T-7 (a later executable builds on these findings without re-verification), T-8 (novel pattern — study has never shelled out to a model backend). T-5/T-6 do NOT fire (read-only diagnostic, no doctrine edit) → **Panel: none.**
**Opened:** 2026-09-03

---

## Walk 0 — context pin (REAL, measured 2026-09-03)

**CEO directive (verbatim intent):** make the study app "a `claude -p` setup so that i can use it locally myself without being tied to the api." The operative words are *locally myself* — read as an opt-in local backend for the CEO's own machine, not a replacement of the API path for anyone else. That reading is routed into the diagnostic as **Fork 1**, to be confirmed rather than assumed.

Every line below is a Planner CLAIM measured at current HEAD (`a87df1e`); the diagnostic agent re-reads each and corrects it if wrong.

1. **Single egress.** `https://api.anthropic.com/v1/messages` appears in exactly three places, all in `src/lib/api.js` (:45, :89, :184) — inside `callClaude` (:39), `callClaudeStream` (:80), `testApiKey` (:181). No other file in `src/` reaches the model. The backend swap therefore has a **two-function seam**, not a codebase-wide one.
2. **Call-site census (13, across 7 files — walk 0 wrote "12" and enumerated 13; corrected at walk 3, see w3-4).** `StudyContext.jsx:1234`, `:1314` (both `callClaudeStream`); `components/study/SkillsPanel.jsx:192`; `lib/conceptLinks.js:122`, `:210`; `lib/syllabusParser.js:191`; `lib/skills.js:254`, `:337`; `lib/extraction.js:852`, `:926`, `:1364`; `lib/study.js:2019`, `:2074`. Every `callClaude` site passes a **single-element** `messages` array; only `StudyContext.jsx:1314` passes a multi-turn history (`chatMsgs`).
3. **No concurrency to preserve.** No `Promise.all`/`allSettled` wraps any model call in `extraction.js`, `skills.js`, `study.js`, or `conceptLinks.js`; the sole `Promise.all` in those files is `skills.js:619` over content-hash checks. Model calls are **sequential**, so a slower per-call backend degrades wall-clock linearly and cannot deadlock on a spawn pool.
4. **Model pinning.** `MODEL_SONNET = "claude-sonnet-4-20250514"` (api.js:22), `MODEL_HAIKU = "claude-haiku-4-5-20251001"` (:23); the `useHaiku` boolean is the only model selector callers have. `callClaudeStream` **hardcodes** `"claude-sonnet-4-20250514"` inline at :98 rather than reading the constant — a pre-existing inconsistency the backend work will have to resolve one way or the other.
5. **Tauri surface.** `src-tauri/capabilities/default.json` already grants `shell:allow-open` plus a `shell:allow-execute` scoped to **two fixed cmd strings** (`soffice`, `/Applications/LibreOffice.app/Contents/MacOS/soffice`) with `args: true`, and an `http:default` scoped to `https://api.anthropic.com/**`. `tauri-plugin-shell = "2"` is in `src-tauri/Cargo.toml:17` and initialised at `src-tauri/src/lib.rs:81`. `lib.rs` registers exactly **one** `#[tauri::command]` (`greet`, :5, handler :91). The fixed-cmd shape of the existing scope entries is the reason Q3 must decide between shell-plugin scoping and a Rust command.
6. **CLI probe — run on this machine 2026-09-03, `claude` 2.1.178 at `/opt/homebrew/bin/claude`:**
   - `echo "Reply with only the word PONG." | claude -p --tools "" --model sonnet --output-format json` → one JSON object; `result` = `"PONG"`, `is_error` false, `stop_reason` `"end_turn"`, `session_id` present, `duration_ms` **2515**, wall **7.58s**, `usage.cache_creation_input_tokens` **15015**.
   - Same call plus `--system-prompt "You are a terse assistant." --disable-slash-commands` → `result` `"PONG"`, `duration_ms` **4023**, `cache_creation_input_tokens` **9092**.
   - **The pin that matters:** a fixed per-call input overhead of roughly **9k–15k tokens survives `--tools ""`**. The direct API path carries none of it. On a subscription this is rate-limit consumption rather than dollars, which is exactly why Q6 measures it rather than pricing it.
7. **`--bare` is disqualified, by its own help text.** It states Anthropic auth is strictly `ANTHROPIC_API_KEY` or `apiKeyHelper` and that "OAuth and keychain are never read." The one flag that would strip the overhead is the one mode that cannot satisfy "without being tied to the api." This makes Q7 (auth mode) the load-bearing question of the whole request, not a detail.
8. **Flags available at 2.1.178** (from `claude --help`): `-p/--print`, `--output-format text|json|stream-json`, `--include-partial-messages` (stream-json only), `--input-format text|stream-json`, `--system-prompt`, `--append-system-prompt`, `--model` (alias or full id), `--tools` (`""` disables all), `--session-id`, `-r/--resume`, `--json-schema`, `--max-budget-usd`, `--no-session-persistence`, `--disable-slash-commands`, `--settings`, `--strict-mcp-config`.
9. **Prior art in-repo:** `docs/planning/python-sidecar-spec.md` already establishes the sidecar-via-`tauri-plugin-shell` pattern for the PDF parser (short-lived process, JSON on stdout, exit). Q3 must say whether the CLI backend is that same pattern or a different one — the difference is that the sidecar is *bundled* and `claude` is *not*.
10. **Nothing in `knowledge/` addresses this.** Grep over `knowledge/` and `docs/` for `claude -p` / `--print` / subscription / "without api key" returns only `docs/buzz-macos-install-guide.md` and the sidecar spec — no prior diagnostic answers any question below.

⚠️ Walk 0 carries no fold rows. Walks 1+ are appended AFTER the draft exists, from real passes.

---

## Walk 1 — five-lens sequential walk (post-draft, real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w1-1 | 1 | Weak spots | does any instruction contradict the step's own read-only constraint? | pre-existing | Q4 told the agent to "build or inspect the packaged app" while the Identity says create no files outside `knowledge/research/`. A `tauri build` writes `target/` and `dist/` — the agent would have to either disobey the constraint or skip the question, and skipping is the likelier failure because the constraint is stated first and in bold | `Determine by evidence (build or inspect the packaged app, or probe the environment a Finder-launched Tauri app sees) whether /opt/homebrew/bin is on that PATH.` | folded: cheap probe ordered FIRST (`launchctl getenv PATH`, then any existing built `.app`), the build permitted only if neither settles it, and its outputs named explicitly as build artifacts that are not deposits and are not committed |
| w1-2 | 1 | Weak spots | is every instruction actually executable as written? | pre-existing | Q5 said "take at least three real prompts from the codebase and run them" — but those prompts are assembled at runtime by template functions over document content, so there is nothing to "take." The agent would either fabricate a prompt and call it real, or stall | `Take at least three real prompts from the codebase — syllabusParser.js:191, skills.js:337, extraction.js:926 — run them through claude -p with --tools "" and a --system-prompt, and report whether the output feeds extractJSON (api.js) cleanly or arrives wrapped in prose/fences.` | folded: reconstruct the three prompts against a short synthetic input the agent writes itself, plus an explicit statement that the measurement is output SHAPE (fences/prose wrapping), not extraction accuracy |
| w1-3 | 1 | Destruction | what can this read-only plan still consume or damage? | pre-existing | the step licenses `claude` probes with no ceiling, while Q6(c) asks for bulk-path timings — the obvious agent reading is to RUN a syllabus import, which spends the CEO's own subscription quota, at ~9-15k input tokens per call, on a question that multiplication answers. Nothing bounded probe count, probe location (a repo-rooted probe loads `CLAUDE.md` and inflates the very overhead figure Q6 measures), or nested tool use | no probe-budget clause existed | folded: a hard-cap clause added ahead of the questions — at most ~10 probes total, all `--tools ""`, all from a scratch dir outside the repo, bulk figures by measured-time x counted-calls and NEVER by executing the import, and env vars set inline per command rather than exported |
| w1-4 | 1 | Vulnerabilities | does the plan make the agent name the new attack surface, or merely notice it? | pre-existing | Q3(v) asked for "the security surface each opens, stated plainly" — satisfiable by one sentence of acknowledgement. The actual surface is specific and new: an app that spawns a user-configurable executable path has an arbitrary-code-execution vector the current HTTPS-only design does not have, and the plan asked for no mitigation | `(v) the security surface each opens, stated plainly.` | folded: the surface named literally in the question, with three candidate mitigations (validate the binary, restrict to a candidate allowlist, accept as dev-only) and a requirement to RECOMMEND one rather than note the risk and move on |
| w1-5 | 1 | Integration-record | is every citation one I verified rather than recalled? | pre-existing | the Gap Assessment was attributed to "Rule 25" from recall; the exact-columns requirement was read in `PLANNER_TEMPLATE.md` but its rule number was not confirmed. Record-class — the columns are given literally, so the agent's behaviour is unchanged either way — but a wrong number is a false citation in a file later plans cite | `Produce a Rule 25 Gap Assessment table with exactly these columns:` | folded: rule number dropped; the literal column set carries the requirement |
| w1-6 | 1 | ACID | does the header's scope statement still match what the step licenses? | fold-introduced (by w1-1) | permitting a conditional `tauri build` falsified the header's `creates only knowledge/research/` — the scope line is read by the agent as the boundary of what it may write, so the two would have disagreed | `**Test Scope:** none (read-only characterization; changes no code, creates only knowledge/research/)` | folded: header restated to deposit-only-to-`knowledge/research/` with the conditional build named and its artifacts excluded from deposits and commits |

**Walk 1 total: 6 findings (instruction 5 / record 1), 1 of 6 fold-introduced. All folded; each fold re-probed for its own text (6 probes, all == 1). Direction verdict: PROCEED — the shape (confirm the seam → settle auth first → transport → discovery → output contract → budget → multi-turn → Gap Assessment + forks) held under all five lenses; every finding was a refinement inside that shape, none a direction-class kill. Bar NOT met (5 instruction-class).**

---

## Walk 2 — five-lens sequential walk (real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w2-1 | 2 | Weak spots | can the load-bearing probe's negative result be trusted? | pre-existing | Q2(b) asks the agent to set a bogus `ANTHROPIC_API_KEY` and report whether the CLI ignores it. A normal answer would be read as "ignored, OAuth used" — but that outcome is identical to a probe that was simply wrong (wrong var name, a CLI reading a different one, a shell that never passed it). The single most load-bearing question in the plan rested on a negative with no positive control | `If any part of (a)-(c) cannot be established by a probe you actually ran, say INCONCLUSIVE and say exactly what probe would settle it — do not infer.` | folded: a required part (e) POSITIVE CONTROL — also run an invocation that SHOULD honour an API key (`--bare`, whose help says it reads `ANTHROPIC_API_KEY` and never OAuth) with the same bogus value and SHOW IT FAILING; only a probe seen failing when it should fail licenses the conclusion when it succeeds |
| w2-2 | 2 | Weak spots | does the measurement match how the app actually behaves? | pre-existing | Q6(b)'s cache question would be satisfied by running the same call twice and reporting a cache hit — but study sends a DIFFERENT system prompt from `syllabusParser`, `skills` and `extraction`, so an identical-repeat hit is a number that never occurs in production. The plan would have produced a favourable, irrelevant figure on the question that decides whether bulk extraction is tolerable | `(b) whether successive calls hit cache (cache_read_input_tokens > 0 on call 2) or each pays creation again — this is the difference between a tolerable and an intolerable bulk path;` | folded: both cases required and reported separately — an identical repeat (best case) AND a second call whose system prompt and body differ, with the reason the second is the production-representative one stated in the instruction |
| w2-3 | 2 | Destruction | is the budget the plan imposes actually spendable on the work it demands? | fold-introduced (by w1-3) | walk 1's "~10 probes" cap is smaller than the questions' own demands: Q2 needs three (now four with w2-1's control), Q5 three, Q6 five to seven, Q4 one — twelve to fourteen minimum. The agent would either overrun silently or drop a question, and dropping is likelier since the cap is bold and stated first | `Run **at most ~10 probes in total across all questions**,` | folded: cap raised to 20 with a per-question allocation (Q2 four, Q4 one, Q5 three plus two retries, Q6 six, remainder spare) and an explicit instruction to name the question and reason rather than quietly overrun |
| w2-4 | 2 | Vulnerabilities | can this read-only step leave the tree dirty? | fold-introduced (by w1-1) | walk 1 permitted a conditional `npm run tauri:build`. A Tauri release build writes `src-tauri/target/` and can update the TRACKED `src-tauri/Cargo.lock` — a read-only diagnostic would end with uncommitted changes to a tracked file it never meant to touch, and the header's scope line would be false | `only if neither settles it may you run npm run tauri:build, whose target//dist/ outputs are build artifacts, NOT deposits, and are not to be committed.` | folded: the build FORBIDDEN outright, with the Cargo.lock reason stated; non-mutating probes only (`launchctl getenv PATH`, plus any already-built `.app`); if neither settles it, write INCONCLUSIVE and name the probe the executable runs at implementation time. Header Test Scope restated to run-no-build, and the "Test"-is-a-button clarification added |
| w2-5 | 2 | Integration-record | do the declared deposit paths resolve where the gate will look? | pre-existing | the Deposits block and the step prose both named `knowledge/research/…` unprefixed. `plan_lint`'s (o1) path check could not resolve it (nor the walk-register reference) at the drafting location, and the two references to the register disagreed in form with the absolute one in the Drafting Cycle block | `- knowledge/research/claude-cli-backend-feasibility-2026-09-03.md` | folded: deposit path and both walk-register prose references project-prefixed to `study/…`; (o1) now resolves the register. NOTE: `plan_lint`'s (o2) still warns because its `KNOWN_PROJECTS` set omits `study` — a tool gap, not a plan defect; the runtime `deposit_exists` gate resolves the prefixed form via its parent-relative strategy |
| — | 2 | ACID | — | — | DRY — one atomic move into `study/knowledge/decisions/`; single step; `auto_close: false` with `pause_for_verdict: after_step_1` matches the diagnostic close pattern; after w2-4 the step mutates nothing at all | — | no fold |

**Walk 2 total: 5 findings (instruction 5 / record 0), 2 of 5 fold-introduced. All folded; 9 substitutions applied, each anchor asserted == 1 before write and each fold re-probed after. Bar NOT met (5 instruction-class). No restructuring fold — every change was to instruction text inside the existing eight-question shape.**

---

## Walk 3 — five-lens sequential walk (real)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w3-1 | 3 | Weak spots | are Q5's targets derived from evidence, or from the Planner's guess? | pre-existing | Q1 produces a table whose "consumer parses JSON?" column identifies which sites the reliability question actually bears on — and Q5 then ignored it, testing three sites the Planner picked before that table existed. If Q1 shows the demanding JSON consumers are elsewhere, the reliability test would run on the wrong three and report a clean result about prompts nobody depends on | `Those prompts are built at runtime by template functions, so RECONSTRUCT three of them — syllabusParser.js:191, skills.js:337, extraction.js:926 — against a short synthetic input you write yourself (a ten-line syllabus, a one-page document),` | folded: Q5's three are now CHOSEN FROM Q1's table (the three JSON-parsing sites with the most structurally demanding output), with the Planner's three named as defaults to keep only if Q1 confirms them and a requirement to say which were chosen and why if not |
| w3-2 | 3 | Destruction | what does a twenty-probe budget leave behind? | fold-introduced (by w2-3) | raising the cap to 20 made a side effect worth naming: every `claude -p` probe writes a session record, so twenty probes bury the CEO's own `/resume` picker in diagnostic noise. Separately, Q5 told the agent to write a synthetic input "yourself" without saying WHERE — the obvious default is the repo it is standing in | `each with --tools "" so no nested agent work can start,` | folded: `--no-session-persistence` added to every probe with the Q7 `--resume` probes named as the one exception; synthetic inputs pinned to the scratch directory outside the repo |
| w3-3 | 3 | Vulnerabilities | can the positive control fail for the wrong reason and still look like a control? | fold-introduced (by w2-1) | w2-1's `--bare` control is only a control if its failure is an AUTHENTICATION failure. `--bare` can also fail on missing context flags or an unreadable settings path — a generic failure would be read as "the control worked," and the load-bearing conclusion would rest on it | `with the same bogus value, and show it FAILING. Only a probe that can be seen failing when it should fail licenses the conclusion when it succeeds.` | folded: the control's error text must be pasted and confirmed to be an authentication error naming the credential; the licensing sentence restated as failing FOR THE RIGHT REASON |
| w3-4 | 3 | Integration-record | does every number in the plan survive being counted? | pre-existing | the plan asserted "twelve call sites" in the Context, in Q1, and in the walk-0 pin — while ENUMERATING thirteen in the same sentence. Counted at HEAD: `grep -rnE 'callClaude(Stream)?\(' src` outside `api.js` returns **13** across 7 files. A Planner count contradicted by the Planner's own list, and Q1's self-correction clause would have made the agent report the discrepancy rather than the plan being right | `Twelve call sites across seven files consume those functions` | folded: corrected to thirteen in all three places (Context, Q1's count and its self-correction threshold, and the walk-0 pin in this register, annotated with the correction) |
| w3-5 | 3 | ACID | is the deposit all-or-nothing across every outcome, including the one that ends the arc? | pre-existing | the fail-honest clause tells the agent to report a Q2 failure at the top of the findings file, but nothing said the file is still deposited when the arc ends there. An agent that concludes "this is not feasible" may reasonably stop — stranding the single most valuable finding and failing the step's own `deposit_exists` gate | `If Q2 establishes that the CLI cannot reliably authenticate off the subscription in a spawned child, say so at the TOP of the findings file — that outcome ends the arc and the CEO must see it first, not on page four.` | folded: deposit required in EVERY outcome, with the arc-ending answer named as the most valuable one the step can produce, remaining questions answered where still answerable and the rest marked N/A-because-Q2 rather than left blank |

**Walk 3 total: 5 findings (instruction 5 / record 0), 2 of 5 fold-introduced. All folded; each anchor asserted == 1 before write. Bar NOT met (5 instruction-class). No restructuring fold. NOTE: three consecutive walks at 5 instruction-class findings is a flat yield, not a converging one — per [[flat-yield-means-a-fixed-examination-axis]] walk 4 changes the examination axis rather than repeating the same five questions over the same text.**

---

## Walk 4 — five-lens sequential walk (real; examination axis CHANGED per the walk-3 note)

Walks 1-3 each read the plan text against the same five lenses and each returned five instruction-class findings — a flat yield, which measures the axis rather than the artifact. Walk 4 holds the lenses but changes what they are pointed AT: the plan's **consumer** (the executable that will cite the findings file without re-verifying it), and the plan's **execution environment** (a Bellows worktree, driven from inside a Claude Code session).

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w4-1 | 4 | Weak spots | does an instruction assume state the execution environment will not have? | pre-existing | Q4 tells the agent to inspect a built `.app` "under `src-tauri/target/`" — but `target/` is git-ignored, so a Bellows worktree contains none regardless of how many builds the main checkout has run. The agent would look, find nothing, and write INCONCLUSIVE while a built app sat in the main checkout | `and, if a built .app already exists under src-tauri/target/, inspect that.` | folded: the git-ignored/worktree asymmetry stated in the instruction, with the main checkout's absolute path given for a read-only look before concluding none exists |
| w4-2 | 4 | Vulnerabilities | is the probe environment the same as the environment being designed for? | pre-existing | the whole of Q2 measures what a spawned `claude` child sees — measured from inside a Claude Code session, whose environment may carry `ANTHROPIC_API_KEY` or `CLAUDE_CODE_*` variables a Tauri app would never hold. The measurement is taken inside a different environment than the one it characterizes, and nothing said so; a clean Q2 result could be an artifact of the harness | no such warning existed | folded: a bolded pre-question clause requiring `env \| grep -iE 'anthropic\|claude'` to be dumped and pasted before Q2, a requirement to state in the findings that every environment result came from inside a Claude Code session, and the design consequence made unconditional — the app CONSTRUCTS the child environment rather than inheriting it, whatever the probes show |
| w4-3 | 4 | Integration-record | what does the consumer of this findings file need that the plan never asked for? | pre-existing | the file is written to be cited by a later executable without re-verification (the reason this diagnostic is T-7) — and roughly half of what it pins is CLI-version-dependent: flags, output field names, the streaming event shape, the per-call overhead. `claude` auto-updates. An executable citing this file months later would edit against a CLI that no longer behaves this way, and nothing in the plan gave it a way to notice | `Deposit the Gap Assessment as the last section so the executable can lift it directly.` | folded: a required `### Verification Blocks` closing section — per load-bearing claim, the claim, the one command that re-checks it, and today's measured value, hung on a recorded `claude --version`, with the instruction that an executable finding a different version re-runs the blocks before editing |
| w4-4 | 4 | Integration-record | can the CEO actually decide the forks from what the plan asks for? | pre-existing | Q8 asked the agent to "say which is cheaper" and "recommend a shape" — satisfiable entirely in adjectives, which have no probe and escape every rule that catches a factual claim. The forks are the CEO-facing output of the whole diagnostic and they were the least specified part of it | `Recommend a shape for each fork, then leave the decision open.` | folded: each recommendation must carry its consequence in a stated UNIT — Fork 1 in files-touched and whether released builds change, Fork 2 in measured wall-clock for one full syllabus import per option, Fork 3 in what `capabilities/default.json` would grant — with "cheaper/safer/cleaner" named explicitly as non-answers |
| — | 4 | Destruction | — | — | DRY — after w2-4 removed the build and w3-2 pinned synthetic inputs to the scratch directory and added `--no-session-persistence`, the step's only side effects are the probe calls themselves, all budgeted and all outside the repo | — | no fold |
| — | 4 | ACID | — | — | DRY — one atomic move into `study/knowledge/decisions/`; w3-5 closed the last hole by requiring the deposit in every outcome including the arc-ending one | — | no fold |

**Walk 4 total: 4 findings (instruction 4 / record 0), 0 of 4 fold-introduced. All folded; each anchor asserted == 1 before write; fold-check clean (5 signals held). Bar NOT met (4 instruction-class). No restructuring fold. The axis change worked — all four are findings the three preceding text-passes structurally could not see, and both destructive lenses went dry.**

---

## Walk 5 — five-lens sequential walk (real; untargeted confirming pass)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w5-1 | 5 | Weak spots | did the folds leave the text saying what they meant? | fold-introduced (by w2-3 and w3-2) | w2-3 replaced the probe-cap clause with a sentence ending in a full stop, orphaning the per-probe requirements that followed it — the paragraph read "…rather than quietly overrunning. each with `--tools ""` …". The three binding per-probe conditions now hung off the overrun sentence rather than off every probe, and an agent could reasonably read them as conditions of asking for more budget | `Spend the budget deliberately; if a question needs more, say which one and why rather than quietly overrunning. each with --tools "" so no nested agent work can start, each with --no-session-persistence so twenty probes do not land in the CEO's /resume history (the ONE exception is any Q7 probe testing --session-id/--resume, which needs persistence by definition — flag those probes as the exception), and each from a scratch directory OUTSIDE this repo (a repo-rooted probe picks up CLAUDE.md and distorts the very overhead number Q6 measures).` | folded: the three conditions promoted into their own bolded sentence — "Every probe, without exception, carries all three of" — with each condition restated as a clause under it |
| w5-2 | 5 | Vulnerabilities | can an instruction added to improve honesty leak a credential? | fold-introduced (by w4-2) | w4-2 required the agent to "dump and paste `env \| grep -iE 'anthropic\|claude'`" into the findings file — which is committed to git. If `ANTHROPIC_API_KEY` is set in the session's environment, its VALUE is a live credential and the fold would have written it into a tracked file. The clause added to make the environment reading interpretable was itself the leak | `dump and paste env \| grep -iE 'anthropic\|claude' so the reading is interpretable,` | folded: names and presence ONLY (`… \| cut -d= -f1`), each reported as `<set, N chars>` or `<unset>`, with the reason stated inline — the file is committed and a pasted value is a leaked key |
| w5-3 | 5 | Integration-record | do the scope statements agree after the path-prefix folds? | fold-introduced (by w2-5) | the Identity still forbade files "outside `knowledge/research/`" while w2-5 moved the deposit path to `study/knowledge/research/` and w3-2 licensed a scratch directory outside the repo. Record-class — both paths resolve and no agent behaviour changes — but three scope statements in one plan disagreeing is exactly what a later reader cites | `create NO files outside knowledge/research/.**` | folded: Identity restated to `study/knowledge/research/` with the scratch directory named as the explicit second location |
| — | 5 | Destruction | — | — | DRY — the only side effects remain the budgeted probes; w5-1's restatement tightened rather than widened them | — | no fold |
| — | 5 | ACID | — | — | DRY | — | no fold |

**Walk 5 total: 3 findings (instruction 2 / record 1), 3 of 3 fold-introduced. All folded; fold-check clean (5 signals held). Bar NOT met (2 instruction-class). No restructuring fold. Every finding this walk was damage from a previous walk's folds — the noise-floor signature, and the reason walk 6 runs.**

---

## Walk 6 — five-lens sequential walk (real; untargeted)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w6-1 | 6 | Vulnerabilities | does the plan forbid the only method by which one of its questions could be answered? | fold-introduced (by w2-4) | Q3(a) asks whether the Tauri `shell:allow-execute` scope grammar admits a variable binary path — a question whose obvious method is to write a capability and build. w2-4 forbade the build. The agent would be left inferring from memory or reaching for network docs, on the question that decides the transport recommendation | `determine whether the scope grammar admits a user-configurable binary path at all (a variable path, a $HOME-relative path, a glob), or whether it forces a hardcoded candidate list.` | folded: pointed at `src-tauri/gen/schemas/desktop-schema.json`, the generated ACL schema already committed in the repo — verified at authoring to contain the `shell:allow-execute` identifier — with an instruction to read and quote the `cmd`/`args`/`sidecar` field types. A local, non-mutating source of truth that the build prohibition does not block |
| w6-2 | 6 | Weak spots | does the question have a single answer, or does it only look like it does? | pre-existing | Q6(c) asked the agent to "count how many sequential model calls a full syllabus import makes" — but several of those calls sit inside loops over chunks, materials and assignments, so the count is a function of input size and no single number is honest. An agent forced to produce one would pick an input silently, and the CEO's before/after would rest on an assumption nobody stated | `(c) count how many sequential model calls a full syllabus import and a full skill extraction actually make (trace syllabusParser.js, skills.js, extraction.js — do not guess), and multiply by measured per-call wall time to give a real before/after for the CEO;` | folded: the answer is required as a FORMULA in each path's input variable with the loop's source location named, then one worked example at a stated input size — assumptions visible rather than silent |
| — | 6 | Destruction | — | — | DRY — side effects unchanged from walk 5; both folds this walk narrow method rather than widening reach | — | no fold |
| — | 6 | Integration-record | — | — | DRY — deposit path, Identity scope, header Test Scope and the Deposits block now all name `study/knowledge/research/`; cycle log matches this register walk for walk | — | no fold |
| — | 6 | ACID | — | — | DRY | — | no fold |

**Walk 6 total: 2 findings (instruction 2 / record 0), 1 of 2 fold-introduced. All folded; fold-check clean (5 signals held). Bar NOT met (2 instruction-class). No restructuring fold.**

---

## Walk 7 — five-lens sequential walk (real; untargeted)

| id | walk | lens | sub_question | origin | finding | pre_fold_text | resolution |
|---|---|---|---|---|---|---|---|
| w7-1 | 7 | Weak spots | is the load-bearing question's first part answerable by the method it names? | pre-existing | Q2(a) said "run a probe and report where the credential comes from" — but `claude -p`'s JSON output carries no field naming the credential source. The probe cannot answer the question, so the agent would infer from the fact that the call succeeded, which is precisely the inference w2-1's positive control exists to forbid. The plan named a method that does not reach its own answer | `Specifically: (a) run a probe and report where the credential comes from;` | folded: (a) now names a method that does reach it — the CLI's `auth` subcommand for account and auth mode, paired with one working `claude -p` call — and states explicitly that `-p`'s output names no credential source, so a probe alone cannot answer (a) |
| — | 7 | Destruction | — | — | DRY — no new side effects; the fold adds a read-only subcommand call inside the existing Q2 probe allocation | — | no fold |
| — | 7 | Vulnerabilities | — | — | DRY — the main-checkout path Q4 names is read-only, and w5-2's value-masking covers the only credential-adjacent output the step produces | — | no fold |
| — | 7 | Integration-record | — | — | DRY | — | no fold |
| — | 7 | ACID | — | — | DRY | — | no fold |

**Walk 7 total: 1 finding (instruction 1 / record 0), 0 of 1 fold-introduced. Folded. Bar NOT met (1 instruction-class) — an instruction-class finding re-opens the walk regardless of the mechanical verdict, which read BAR_MET at walk 6 on its plateau heuristic while two instruction-class findings stood. Four of five lenses dry.**

---
